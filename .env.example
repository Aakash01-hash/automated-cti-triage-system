# Optional live enrichment keys. The app runs without these, but live lookups
# will return "not configured" until the values are set.
ABUSEIPDB_API_KEY=
VT_API_KEY=

# Optional when running scripts from outside the repository root.
PYTHONPATH=.
